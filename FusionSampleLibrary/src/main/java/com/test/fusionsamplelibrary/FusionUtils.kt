package com.test.fusionsamplelibrary

import android.content.Context
import android.widget.Toast

object FusionUtils {

    fun starBugReporting(context: Context) {
        Toast.makeText(context, "Started bug reporting", Toast.LENGTH_SHORT).show()
    }
}